/*
  Warnings:

  - A unique constraint covering the columns `[key]` on the table `key` will be added. If there are existing duplicate values, this will fail.

*/
-- CreateIndex
CREATE UNIQUE INDEX "key_key_key" ON "key"("key");
